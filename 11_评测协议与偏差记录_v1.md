# HeurAgenix 复现：评测协议与偏差记录 v1（2026-09-22）

## 0. 当前状态（一句话）
训练已完成（dual final_lora 2026-09-22 02:00，vanilla final_lora 00:01）；原定 04:22 定时评测因脚本路径错误失败（已修复）；现已在服务器 225 上用 8 worker / 4 GPU 启动 **k=0 评测（无 TTS）**，覆盖 3 个选择器 × 13 个 TSPLIB 实例。

## 1. 训练与落盘结果
- `models/train_dual_full/final_lora`（dual / POR+CPR）：324MB，2026-09-22 02:00
- `models/train_vanilla_full/final_lora`（vanilla GRPO）：324MB，2026-09-22 00:01
- 评测脚本：`repo/eval_selector_tsp.py`（已支持 `--tts-budget / --time-limit / --stale-limit / --max-new-tokens / --fallback-complete`）
- 备选 Qwen2.5-7B-Instruct-1M base：`models/Qwen2.5-7B-Instruct-1M`

## 2. 为什么主评测先用 k=0（关键工程发现）
论文 Appendix 求解设置为：M=5，Monte-Carlo search times = 10，单实例限时 2h；TTS 协议为“先执行候选 M=5 步，再随机采样直至完成，重复 T 次取平均终端代价”。我们按原始 released 代码（`src/util/tts_bon.py` + `RandomHyperHeuristic`）测了单次随机完成 rollout 的真实耗时（单核，n=节点数，rollout 上限 2n 步）：

| 实例 | n | 单次 rollout 实测/估计 | 每轮 TTS（3 候选 × 10 rollout）单核耗时 |
|---|---|---|---|
| kroC100 | 100 | 0.27 s | ~8 s |
| kroA100 / kroB100 | 100 | 1.1 s | ~33 s |
| bier127 | 127 | 3.9 s | ~2 min |
| kroA150 | 150 | 6.6 s | ~3 min |
| pr152 | 152 | 5.8 s | ~3 min |
| kroB200 | 200 | 14.3 s | ~7 min |
| tsp225 | 225 | 23.6 s | ~12 min |
| a280 | 280 | 43.6 s | ~22 min |
| pcb442 | 442 | 341 s | ~2.8 h |
| gr666 | 666 | 1523 s | ~12.7 h |
| pr1002 | 1002 | 估计 1.5 h 级 | 数十小时 |
| pr2392 | 2392 | 估计 6 h 级 | 无法在 2h 内完成 |

结论：若严格按论文 k=10 协议，n≥442 的实例单轮 TTS 已超过 2h 限时，pr1002/pr2392 完全不可行（即使 8 卡并行）。因此采用两阶段策略：
- **阶段 A（已启动）**：全量 k=0，即模型直接给候选，取第一候选执行；保证 2h/实例内出全部表格行，作为“无 TTS 下界”。
- **阶段 B（待阶段 A 释放 GPU）**：对 n≤225 的实例补跑 k=10（必要时用多进程并行 TTS），用于 TTS 增益 / Figure 9 口径对照。

## 3. 阶段 A（k=0）协议
- 3 选择器：dual（POR+CPR LoRA）、vanilla（GRPO LoRA）、raw（Qwen2.5-7B-Instruct-1M base）
- 13 实例：kroA100 kroA150 kroB100 kroB200 kroC100 bier127 tsp225 a280 pcb442 gr666 pr152 pr1002 pr2392
- 每轮：模型输出卡片+至多 3 候选 → k=0 取第一候选 → M=5 步；轮数上限 int(2n/5)（与原始代码 selection_round*M <= 2n 一致）
- 安全阀：若当前解未完成且第一候选是 refinement（_2opt/_3opt/SA），改用候选中的 construction，否则用 nearest_insertion；完解后连续 10 轮无改进则停（stale-limit=10）
- 2h/实例 timeout；若 timeout 时解未完成，用 nearest_insertion 补完成（`fallback_complete=true`，报告中必须标注）
- 温度 0.7 / top-p 0.95 / max_new_tokens 512，4bit NF4，与训练推理一致

## 4. 运行方式与产物
- 启动：`tmux new -s eval_k0 "bash repo/run_eval_k0.sh"`（服务器 225，2026-09-22 13:13:34）
- 8 worker，`CUDA_VISIBLE_DEVICES = i % 4`；任务清单 `results/eval_full/parts/tasks_{0..7}.txt`
- 单任务产物：`results/eval_full/parts/{mode}__{instance}.json` 与 `logs/{mode}__{instance}.log`
- 合并：`envs/heura/bin/python repo/combine_parts.py` → `results/eval_full/k0_{dual,vanilla,raw}.json` 与 `k0_combined.csv`
- 监控：`tail results/eval_full/parts/eval_k0_master.log`；`ls results/eval_full/parts/*.json`
- 断点续跑：runner 对已存在且非空的 part JSON 自动跳过

## 5. 已知偏差（写报告必须声明）
1. 主协议 k=10 在 n≥442 上不可行；阶段 A 全部为 k=0，不能直接对标论文 Table 3/4 的数字（论文平均 gap：raw 5.01 / vanilla GRPO 4.39 / dual 0.59）。
2. timeout 实例用 nearest_insertion 补完成，属于工程兜底，不是论文协议。
3. 完解后 stale-limit=10 提前停，原始代码是跑到 2n/5 轮；影响为少跑最多 10 轮改进。
4. 训练数据为 released code 规则重建的 1109 条，非论文精确数据；这会影响选择器质量。
5. P2 池中 `_2opt_89aa` 实测对完整解 300 次调用无改进；`_3opt_e75b` 300 次可把 kroA100 从 22239 改善到 21490（0.98%），说明池内局部搜索强度有限，k=0 对选择顺序更敏感。

## 6. 后续 TODO
- [ ] 阶段 A 完成后合并 CSV，写 Table 3/4 复现报告（区分 k=0 口径 vs 论文 k=10 口径）
- [ ] 阶段 B：n≤225（约 8 个实例）跑 k=10；优先 dual，再 raw/vanilla；用于 TTS 增益对照
- [ ] Figure 9：pr152 上 k=0/1/2/4/8/10（若时间允许，多进程并行 TTS）
- [ ] B 档 DeepSeek v4.1 flash 的演化/Table 6（DEEPSEEK_LAB_API_KEY，deepseek-chat→deepseek-flash）
- [ ] EDA 迁移 Phase 0 等用户拍板
