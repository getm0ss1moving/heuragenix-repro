# HeurAgenix 复现执行决策记录（2026-09-20）

## 一、用户拍板的决策（来自本轮对话）

1. **LLM API**：不使用 GPT-4o / Azure OpenAI；改用 **DeepSeek v4.1 flash API**（`deepseek-chat`，经本地凭证接口）承担：
   - 启发式演化（原 GPT-4o 的位置）
   - LLM 在线选择（Table 6 / Figure 8）
   - 涉及模型更新的 baseline（EoH、ReEvo 等）
2. **代码 freeze**：接受
   - 核心实验 = `f9ec60f`（备选 `de8a9cf`，两者 evolved heuristics 一致）
   - 训练代码 = 历史 commit `6e4fc66` 的 `src/training/*` + 数据收集器
3. **训练数据**：接受“按 released code 规则重建”离线数据与 JSON（论文未发布精确训练数据）。
4. **启发式池口径**：先用 **P2**（Appendix E 确定性基础启发式 + 3 个 evolved），再做 P1/P3 校准。
5. **多卡并行**：允许占用多张空闲卡训练/eval（同端口不同 GPU 或多端口）。
6. **项目目录**：服务器大容量盘新建 `/Data/dzy/heura_repr`。
   - 实际服务器无 `/Data`（Linux 大小写敏感），大容量挂载点为 `/data`；
   - 已在 `thinklab-105-224` 创建 **`/data/dzy/heura_repr`**（等价路径），并从 `$HOME/heura_repr` 建软链。
7. **外部 baseline 口径**：
   - 不涉及模型更新的 baseline：可直接用论文测试结果作为参考（标注来源）；
   - 涉及模型更新的 baseline：需要同步重跑/更新结果，不能直接引用旧数字。

## 二、服务器侦察结果（只读）

| 端口 | 主机 | GPU | 空闲情况 | 大容量盘 | 备注 |
|---|---|---|---|---|---|
| 224 | thinklab-105-224 | RTX 3090 ×2 可用（GPU0 异常，GPU1/2 正常） | 几乎全空 | `/data` 366G free；`/` 278G free | **已选为主训练节点** |
| 225 | thinklab-105-225 | RTX 3090 ×4 | 全空 | `/data` 86G free；`/` 83G free | 可作第二 eval/训练节点 |
| 227 | thinklab-105-227 | RTX 3090 ×2 可用（GPU0 异常） | 空闲 | `/home` 193G free；`/` 134G free | 备选 |
| 231 | think4proj-105-232 | RTX 4090 ×5 | 部分卡占用，约 6–20G 空闲 | `/` 11G free；NAS 148G free | 驱动 550（CUDA 12.4），适合 vLLM；存储紧张 |
| 232 | think4proj-105-231 | GPU 查询挂起 | 未知 | `/` 63G free；NAS 148G free | 谨慎使用 |
| 234 | think4proj-105-234 | NVIDIA 驱动通信失败 | 无 | `/` 396G free | 可作 CPU 数据/存储节点 |
| 223/226/228/229/230/233 | — | — | SSH 连接关闭/超时 | — | 暂不可用 |

**主节点选择理由**：`/data` 剩余 366G，满足模型 15GB + checkpoint + 数据 + 反复实验的存储要求；2×RTX 3090 24G 可并行训练/评测；网络可达 GitHub / hf-mirror / 清华 PyPI。

## 三、技术栈与已发现的约束

- 主节点驱动 `470.256.02`，**不能直接跑 CUDA 12.1/12.4 编译的 vLLM 新版本**。
- 应对方案：
  1. 训练/推理先用 **PyTorch cu118** 栈 + `transformers` + `peft` + `trl`（`use_vllm=False`，HF generate）；
  2. 若必须用 vLLM 加速 Table 3/4 推理：改用端口 231 的 RTX 4090 + 驱动 550，模型与结果通过 `/mnt/nas2` 或 `rsync` 在节点间共享（注意 NAS 剩余 148G）；
  3. 训练优先尝试 bf16 LoRA（24G 可能 OOM），失败则 QLoRA 4-bit，并在报告中标注与论文差距。
- 已创建服务器目录：
  ```
  /data/dzy/heura_repr/{repo,models,datasets,output,results,logs,envs,evidence}
  ```
- 已开始：
  - clone `microsoft/HeurAgenix` 完整历史到 `repo/HeurAgenix`；
  - worktree `HeurAgenix_paper` (f9ec60f)、`HeurAgenix_paper_alt` (de8a9cf)；
  - 提取训练代码到 `repo/HeurAgenix_training` (6e4fc66)；
  - rsync HF 数据集 27MB 到 `datasets/raw_HeurAgenix`；
  - 在 tmux `heura_env2` 中安装 Miniconda + Python 3.11 + huggingface_hub。

## 四、下一步执行顺序（已按 A 档优先）

1. 完成 Python 环境（cu118 torch + transformers/peft/trl/datasets/spacy/langdetect）。
2. 下载 Qwen2.5-7B-Instruct-1M 到 `/data/dzy/heura_repr/models`。
3. 按论文精确 split 整理数据（TSP/CVRP/MKP/JSSP/MaxCut）。
4. 构建 P2 heuristic pool；跑 Table 5 确定性复现（无需 LLM）。
5. 按 released code 规则收集 TSP 离线数据，生成 dual / vanilla GRPO JSON。
6. 训练 Qwen LoRA/QLoRA 选择器（POR+CPR 与 vanilla），处理 24G 显存限制。
7. 评测 Table 3/4、Figure 9。
8. 使用 DeepSeek v4.1 flash API 复现 Table 6 与演化（B 档）；更新涉及模型更新的 baseline。
9. 汇总为 `复现报告.md` 与一键重跑脚本。


## 五、执行更新（2026-09-20 晚）
- 224 端口 GPU0 硬件异常，导致 CUDA 初始化失败（即使 CUDA_VISIBLE_DEVICES 也不行）；主训练节点改为 **225（thinklab-105-225）**。
- 225：4×RTX3090 24G 全空闲，driver 510.54；torch 2.5.1+cu118 实测 CUDA 可用；/data 64G、/ 83G。
- 项目目录：/data/dzy/heura_repr（服务器实际挂载为小写 /data；用户写的 /Data 不存在）。
- 已完成：代码 worktree（f9ec60f/de8a9cf/6e4fc66）、HF 数据 split curation、P1/P2/P3 池、Python 3.11 + torch cu118 + transformers/trl/peft 环境、Qwen2.5-7B-Instruct-1M 15G。
- DeepSeek API 已验证：https://api.deepseek.com，deepseek-chat 实际返回 deepseek-flash。
- 已验证：nearest_neighbor_e8a4 在 kroA100 上 gap=4.4968%（论文 4.5）；Table5 batch 在跑。
- 已验证：4-bit QLoRA + TRL 0.16.1 GRPO 冒烟训练成功（3090，1 step）。
- 正在后台跑：Table5 全量、TSP 离线数据收集（best+random，search_time=100）。
- 下一步：写 JSON builder，训练 dual/vanilla GRPO，复现 Table3/4/Figure9；B 档用 DeepSeek v4.1 flash。
