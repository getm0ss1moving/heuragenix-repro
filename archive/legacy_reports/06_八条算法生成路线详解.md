# 八条算法生成路线详解：从概念到 EDA 落地

**日期：** 2026-09-20  
**配套：** `05_算法生成与在线筛选备选路线.md`  
**适用范围：** 本文只详解 `05` 第 1 节的 **8 条算法/启发式生成路线**；在线筛选的 10 条路线见文末附录和 `05`，如需逐个展开可另开文件。  
**前置条件：** 已读 `04_EDA迁移与Skill自进化设计建议.md` 与 `05`；会基础 Python；知道 OpenLane/OpenROAD 的 `config.json`、`reports/metrics.csv`、checkpoint 概念。  
**参考值说明：** 文中出现的 trials 数、种群大小、学习率等数字都是**示例起点，来自公开实现或经验，不是理论最优**，必须用你自己的 `pilot` 数据校准。  
**术语约定：** 一次“评估”= 跑一次 flow 或一个局部 stage；`budget`= 允许的评估次数/时间/工具调用数；`fidelity`= 评估保真度（小设计/局部 stage 是低保真，全 flow signoff 是高保真）。

---

## 0. 先建立总图：这 8 条路线分别在干什么

| 路线 | 搜索/生成的东西 | 一句话 |
|---|---|---|
| 1 黑盒参数搜索 | 工具参数向量 | 调旋钮，不碰算法代码 |
| 2 逐实例算法配置 ISAC | 参数配置的选择器 | 学“什么设计配什么参数” |
| 3 语法/程序搜索 | 表达式树 / 规则 / DSL | 让计算机进化出新规则 |
| 4 Quality-Diversity | 一个多样化的策略档案 | 不只找一个最优，而是按场景存一队能手 |
| 5 LLM 程序进化 | 代码 / 受限 DSL 程序 | LLM 提案，verifier 筛选 |
| 6 RL / 离线 RL | 序贯策略网络 | 把“下一步选什么”当游戏打 |
| 7 自动算法调度 | 阶段 × 参数 × 时长的 schedule | 生成一条“先怎么跑、再怎么调”的曲线 |
| 8 形式化合成 + 案例复用 | 满足规格的小程序 / 历史案例 | 用约束求解器或近邻检索得到可靠算子 |

它们不是互斥的：真实系统通常是“路线 1/2 先建立强基线，路线 4/7 建库和调度，路线 3/5 做结构创新，路线 6 在数据足够后接管选择”。

---

## 路线 1：黑盒参数/配置搜索（Algorithm Configuration / Hyperparameter Optimization）

### 1.1 一句话定位与类比

**定位：** 把 EDA 工具当成一个黑盒函数 `y = f(parameters)`，只通过“跑一次、看结果”来调参数，目标是让 `y`（PPA、gate、runtime 的效用）最好。  
**类比：** 调收音机的旋钮找清晰频道；你不知道收音机内部电路，只根据“声音清不清”来转旋钮。

### 1.2 最小操作流程

1. **定义搜索空间**：把每个可调项写成 `name → 类型/范围/步长/默认值`。
2. **定义评估函数**：`evaluate(config) -> metrics`，内部做三件事：写 OpenLane config、跑 flow、解析 `metrics.csv`/DRC/LVS/STA。
3. **给预算**：`n_trials`、总 wall-clock、最大并行 worker 数。
4. **采样**：按所选算法提出下一个/下一批 `config`。
5. **记录**：每次 trial 必须落盘 `config + seed + design + metrics + runtime + log 路径`。
6. **选优与复验**：对 top-k 配置在高保真设置上复验，防止低保真过拟合。

### 1.3 最小伪代码（示意）

```python
space = {
    "PL_TARGET_DENSITY": ("float", 0.40, 0.80),
    "PL_WIRELENGTH_COEF": ("float", 0.0, 1.0),
    "GRT_ADJUSTMENT": ("float", 0.05, 0.50),
    "GRT_OVERFLOW_ITERS": ("int", 1, 10),
}

def objective(config):
    run_dir = run_openlane(design="spm", config=config, seed=0)
    metrics = parse_metrics(run_dir)        # WNS/TNS/power/area/DRC/LVS
    return utility(metrics)                 # gate 不过则给大惩罚

records = []
for trial in range(20):
    config = propose_next(records)          # 由 Random/TPE/SMAC/CMA-ES 决定
    score = objective(config)
    records.append((config, score))
best = select_best(records)
signoff_recheck(best, top_k=3)
```

### 1.4 方法家族与关键参数

| 方法 | 机制 | 适合 | 关键参数 |
|---|---|---|---|
| Random Search | 均匀/对数随机采样 | 基线；低维 | trials、seed、范围 |
| Grid Search | 笛卡尔积 | 维度不高于 3、水平少 | 每维水平数 |
| TPE（Optuna） | 用 `P(x|y)` 核密度估计“好点”分布，交替采样/更新 | 中小维度、混合类型 | `n_startup_trials`、`n_ei_candidates`、`multivariate` |
| SMAC3 | 随机森林 surrogate + EI/LogEI，支持条件空间 | 离散/类别/条件参数 | `walltime_limit`、`n_trials`、`intensifier` |
| BOHB | BO + Hyperband，多保真 | 评估昂贵、可早停 | `min_budget`、`max_budget`、`eta` |
| DEHB | 差分进化 + Hyperband | 并行、高维 | `brackets`、`eta`、`mutation_factor` |
| CMA-ES | 维护高斯分布（均值+协方差），按排名更新 | 连续变量、5–50 维 | `sigma0`、`popsize`、`tolx` |
| PBT | 多配置并行训练/评估，差者复制优者并变异 | 多阶段/非平稳流程 | population、exploit interval、mutation |

### 1.5 在 EDA / OpenLane 里怎么落地

**可搜索参数候选：**
- placement：`PL_TARGET_DENSITY`、`PL_WIRELENGTH_COEF`、`PL_BASIC_PLACEMENT`、`PL_TIME_DRIVEN`、`PL_ROUTABILITY_DRIVEN`、`PL_SKIP_INITIAL_PLACEMENT`、`GPL_CELL_PADDING`。
- routing：`GRT_ADJUSTMENT`、`GRT_LAYER_ADJUSTMENTS`、`GRT_OVERFLOW_ITERS`、`GRT_ALLOW_CONGESTION`、`GRT_ANT_ITERS`、`GRT_ANT_MARGIN`。
- CTS/STA 相关：buffer 策略、时钟约束、repair timing 的开关与迭代。
- 注意：不同 OpenLane 版本的参数名与取值含义可能变化，写进搜索空间前先用 `config.json` 和版本文档核对；**适用范围只对当前镜像/版本成立**。

**评估函数建议：**
- 低 fidelity：小设计（如 `spm`、`gcd`）或只跑到 global route。
- 中等 fidelity：中等设计 + placement + routing + 解析 overflow。
- 高 fidelity：完整 flow + DRC/LVS + STA；只在 top-k 上跑。
- 评分：硬 gate + 归一化 PPA 向量；不要只返回 HPWL。

### 1.6 适用范围、前置条件与常见坑

**适用：** 参数空间清晰、评估成本相对可控、需要快速拿到工程收益。  
**前置条件：** 能自动化运行 flow、能稳定解析指标、有 checkpoint/日志。  
**常见坑：**
- 评估噪声大：固定 seed、同一机器、随机化 trial 顺序、重复关键点。
- 搜索空间过大：先做单变量敏感性扫描，砍掉无影响参数。
- 指标未归一化：不同设计的 WNS/area 量纲不同，先转相对改进再聚合。
- 只报最好 trial：必须报均值/中位数/最差、置信区间和总预算。
- 低保真过拟合：低 fidelity 选出的 top-k 必须在高 fidelity 复验。

### 1.7 最小验收标准

相同总预算下，相比默认配置在同一组留出设计上有统计显著的提升；同时 gate 通过率不下降；所有 trial 可追溯、可重放。

---

## 路线 2：逐实例算法配置（ISAC, Per-Instance Algorithm Configuration）

### 2.1 一句话定位与类比

**定位：** 不找“一组万能参数”，而是训练一个模型：`design_features -> best_config`，给每个实例选配置。  
**类比：** 医生按病人的年龄/体重/病史开不同剂量；而不是给所有病人开同一种药。

### 2.2 它和路线 1 的关系

路线 1 找全局最优配置 `config*`；ISAC 学映射 `π(features) -> config`。  
如果不同设计的最优配置差异很大，全局最优会被少数设计拖累；ISAC 可以按设计族分别选配置。ISAC 也常被看作“算法选择问题（Algorithm Selection Problem）”的配置版。

### 2.3 最小操作流程

1. **选设计特征**：cell 数、macro 数、面积、初始利用率、时钟频率、路径深度、max fanout、初始拥塞估计、设计族标签。
2. **跑性能矩阵**：对每个训练设计，跑若干候选配置（例如 5–10 组），记录 gate + PPA + runtime。
3. **训练预测器**：
   - 回归：预测每个配置的 PPA 分数；
   - 排序：学习配置之间的成对偏好（更稳，类似 POR 的思想）；
   - 分类：直接预测 best config 类别。
4. **验证**：在留出设计上计算 `regret = best_possible_score - selected_score`，报告均值与最差。
5. **部署**：提取特征 → 模型预测 → 选配置 → 后续可再接 bandit 在线微调。

### 2.4 最小伪代码（示意）

```python
# 1) 收集矩阵
rows = []
for design in train_designs:
    feats = extract_features(design)
    for cfg in candidate_configs:
        m = run_openlane(design, cfg)
        rows.append({"design": design, "features": feats, "config": cfg,
                     "gate": m.gate, "score": m.score, "runtime": m.runtime})

# 2) 学排序模型（示意）
model = train_ranker(rows, features_key="features", group_key="design",
                     label_key="score")

# 3) 部署
def select_config(design):
    feats = extract_features(design)
    return max(candidate_configs, key=lambda c: model.predict(feats, c))
```

### 2.5 关键参数与注意事项

- 候选配置数量和覆盖度：太少学不出差异；太多评估成本高。
- 特征必须在“跑 flow 之前”就能拿到，不能用 flow 结束后的指标做输入（信息泄漏）。
- 排序/成对训练通常比绝对值回归更稳；可参考 POR 的排序偏好思想。
- 设计族划分必须跨族留出，不能把同一设计的不同随机种子分到 train/test。

### 2.6 在 EDA 里怎么落地

- 对 small/medium/large 设计分别选 `PL_TARGET_DENSITY` 和 `GRT_LAYER_ADJUSTMENTS`。
- 对 timing-critical 与 congestion-critical 设计分别选 placement 策略。
- 把 ISAC 输出当作在线 selector 的“离线先验”：bandit/LLM 可以在此基础上微调。

### 2.7 适用范围、前置条件与常见坑

**适用：** 设计异构明显、已有一定量跨设计数据、推理预算紧。  
**前置条件：** 能批量跑 flow、有稳定的设计特征提取、有候选配置库。  
**常见坑：** 训练设计太少导致过拟合；特征分布漂移；候选配置库不包含真正的好配置；只看平均而忽略最差设计。

### 2.8 最小验收标准

在留出设计族上，ISAC 的 per-instance regret 显著低于“全局最优配置”，且 runtime/推理成本可接受；如果打不过全局最优，说明设计异质性不足或特征无信息。

---

## 路线 3：语法/程序搜索（GP / Grammatical Evolution / Cartesian GP）

### 3.1 一句话定位与类比

**定位：** 让计算机在“规则/程序空间”里进化，而不只是调数值参数；产物通常是一个可读的表达式、代价函数或决策规则。  
**类比：** 遗传育种：保留表现好的个体，让它们交叉、变异，几代之后可能长出人类没写过的规则。

### 3.2 三种常见表示

| 表示 | 是什么 | 例子 |
|---|---|---|
| GP 树 | 表达式树：内部节点是算子，叶子是特征/常数 | `max(0.5*overflow, via_cost) + 2*layer_penalty` |
| Grammatical Evolution | 用整数基因组按语法规则展开成程序 | 语法：`expr -> expr + term | term` |
| Cartesian GP | 有向无环图，节点可按拓扑复用 | 允许多个中间量共享，结构更紧凑 |

### 3.3 最小操作流程

1. **定义 DSL（非常重要）**：
   - 算子集：`add/sub/mul/div_safe/min/max/if_less/exp/log/sqrt`。
   - 特征集：`overflow`、`wirelength`、`pin_count`、`slack`、`layer`、`congestion`、`via_cost`。
   - 类型系统与安全边界（例如 `div_safe` 防止除零，输出必须落在合理范围）。
2. **定义 fitness**：在训练场景上执行规则，得到 gate + PPA + runtime 的效用；不适用的个体给惩罚。
3. **初始化种群**：随机树/基因，限制初始深度，避免一开始就巨大。
4. **选择/交叉/变异/精英保留**。
5. **验证**：在留出设计上评估；检查规则可读性与是否退化。
6. **归档**：保存规则表达式、fitness、失败案例、适用设计族。

### 3.4 最小伪代码（示意）

```python
def fitness(rule):
    if not is_safe(rule):
        return -inf
    scores = []
    for design in train_designs:
        result = run_with_rule(design, rule)   # 规则嵌入 router/placer
        scores.append(utility(result))
    return mean(scores) - runtime_penalty(rule)

population = initialize(dsl, size=100, max_depth=6)
for generation in range(30):
    scored = [(fitness(ind), ind) for ind in population]
    elites = top_k(scored, k=10)
    children = crossover_and_mutate(elites, rate=0.2)
    population = elites + children
best_rule = best(population, key=fitness)
```

### 3.5 关键参数

- `population size`：50–500（预算越大可越大）。
- `generations`：20–100；但受评估预算限制。
- `max tree depth`：4–8；太深易过拟合、runtime 高。
- 交叉率 0.6–0.9，变异率 0.05–0.3（示例起点）。
- 精英保留数、锦标赛大小、parsimony 系数（惩罚树大小）。
- 随机种子数：每个个体至少评估 2–3 个 seed，降低偶然性。

### 3.6 在 EDA 里怎么落地

- **maze routing 的边代价函数**：规则输入是 gcell/edge 特征，输出代价；fitness 用 global route overflow + wirelength + runtime。
- **net ordering 排序键**：规则输出每条 net 的优先级分数。
- **placement 局部搜索的 move acceptance 规则**：规则输入 `delta_hpwl`、`delta_congestion`、`delta_timing`，输出接受/拒绝。
- **RRR 的拆线选择规则**：规则输出下一个 rip-up 的 net 或区域。
- 规则执行器最好用 Python 解释器 + 深度/时间限制，且只能调用 allowlist API；不要让 GP 生成的表达式直接执行任意 Python。

### 3.7 适用范围、前置条件与常见坑

**适用：** 你要生成的是“评分函数/决策规则”，而不是坐标；有明确 DSL 和快速评估器。  
**前置条件：** DSL 设计、fitness 定义、训练/验证划分、安全执行器。  
**常见坑：**
- 适应度函数太慢：先用小设计/代理评估，再做高保真复验。
- DSL 太强：等价于任意代码，风险高；DSL 太弱：搜不出新规则。
- 树膨胀（bloat）：必须加复杂度惩罚。
- 过拟合训练设计：报告跨设计族泛化。
- 可读性下降：定期人工审查 top 规则，删除无物理意义的项。

### 3.8 最小验收标准

在留出设计上，GP 规则优于人工规则或默认规则，且规则表达式可读、runtime 可控；否则先修 DSL/fitness，不要急着扩大种群。

---

## 路线 4：Quality-Diversity / MAP-Elites / QD-RL

### 4.1 一句话定位与类比

**定位：** 不只找一个“全局最优解”，而是维护一个**按行为特征分桶的多样化解库**；每个桶里存当前最好的策略/算法。  
**类比：** 组建一支队伍：不是只选一个冠军，而是按身高、速度、打法各留一个最强选手，比赛时按对手类型派人。

### 4.2 核心概念

- **Behavior Descriptor（BD）**：描述一个解“长什么样”的坐标，例如 `(utilization, congestion_level)`、`(design_size, macro_count)`、`(runtime_budget, design_family)`。
- **Archive**：BD 空间的网格/质心集合；每个 cell 存一个 elite（当前该行为区域的最优解）。
- **Illuminate**：目标不是单项最优，而是覆盖尽可能多的 cell 并提升每个 cell 的质量。
- **QD-score**：整体档案的覆盖度 + 质量；比只看单点最优更稳健。
- **MAP-Elites**：最简单的 QD 算法：网格 archive + 变异。
- **CVT-MAP-Elites**：用 Voronoi 质心代替网格，适合高维 BD。
- **QD-RL**：把 QD 思想与 RL 结合，学一个能填满 archive 的策略。

### 4.3 最小操作流程（MAP-Elites）

1. 选 BD（1–3 维；先少后多）。
2. 划分 archive（例如 10×10 网格）。
3. 随机初始化一批解/策略。
4. 对每个解评估质量 + 计算 BD。
5. 若该 cell 为空或新解质量更高，就写入 archive。
6. 从 archive 随机选一个 elite，做变异/交叉/参数扰动，回到第 4 步。
7. 重复到预算用尽；输出整个 archive，而不仅是最优解。
8. 用 archive 作为 selector 的动作库：推理时看当前设计落哪个 BD cell，取对应 elite。

### 4.4 最小伪代码（示意）

```python
archive = {}                            # cell -> (quality, solution)
for solution in initial_solutions:
    quality = evaluate(solution)
    cell = behavior_descriptor(solution)
    archive[cell] = keep_better(archive.get(cell), (quality, solution))

while budget_left():
    parent = random_elite(archive)
    child = mutate(parent)
    quality = evaluate(child)
    cell = behavior_descriptor(child)
    archive[cell] = keep_better(archive.get(cell), (quality, child))
```

### 4.5 关键参数

- BD 的维度、范围和分辨率：先 2 维，分辨率 10×10 或 20×20。
- Archive 大小：直接影响评估预算。
- 变异强度：太小探索不足，太大破坏好解。
- 质量函数：gate 不通过的解不能进 archive，或给极端惩罚。
- 批次评估：QD 需要大量评估，必须并行/多保真。

### 4.6 在 EDA 里怎么落地

- BD 示例：`(design_family, congestion_severity)`、`(utilization_bucket, timing_pressure)`、`(macro_count_bucket, routing_layers)`。
- 每个 cell 存：最好的 placement/routing 配置、skill 序列、参数。
- 在线选择：先预测当前设计 BD，然后从对应 cell 或邻近 cell 取候选策略；再用 bandit/LLM 做最终选择。
- 这正好呼应“不同设计族需要不同策略”的 EDA 事实，比单一全局最优更稳。

### 4.7 适用范围、前置条件与常见坑

**适用：** 设计异质性强、你愿意用一个“策略库”而非单个最优策略；评估可以并行/低保真。  
**前置条件：** 能定义合理的 BD；有足够评估预算；有 archive 到生产 selector 的接口。  
**常见坑：**
- BD 选择失败：如果 BD 与性能无关，archive 就是在存随机垃圾。
- 高维 BD 导致 archive 极稀疏；先用 2 维。
- 评估预算不够：QD 是“用数量换多样性”，预算紧时先用路线 1/2。
- 不同 cell 之间的质量不可比：要按 cell 内的相对改进评价。

### 4.8 最小验收标准

在留出设计上，从 archive 按 BD 取策略优于单一全局最优；archive 覆盖率达到目标；不同 cell 的精英策略确实不同（可解释差异）。

---

## 路线 5：LLM 程序进化（FunSearch / EoH / ReEvo / AlphaEvolve / HeurAgenix / Skill 自进化）

### 5.1 一句话定位与类比

**定位：** 用 LLM 充当“程序变异器”：读问题描述、当前程序、性能与失败样例，生成新候选程序；再用 verifier/评估器筛选，保留好的进入下一代。  
**类比：** 一个会写代码的实习生 + 一个严格的测试主管。实习生大量提案，主管用真实工具做筛选和回归测试。

### 5.2 与经典 GP 的区别

| 维度 | GP/语法进化 | LLM 程序进化 |
|---|---|---|
| 变异算子 | 交叉/随机变异 | 自然语言指令 + 代码编辑 |
| 先验知识 | 几乎没有 | 可读文档、可复用已有代码模式 |
| 搜索空间 | 由 DSL 限制 | 通常更大，必须额外限制 |
| 成本 | 评估昂贵 | token + 评估都昂贵 |
| 解释性 | 规则可读 | 需人工审查 rationale 与代码 |

### 5.3 最小操作流程

1. **准备种子程序**：例如一个可运行的 net ordering 规则、RRR 策略、OpenLane 参数调度模板。
2. **评估种子**：在训练场景跑，得到 gate + PPA + runtime + 失败样例。
3. **构造 prompt**：
   - 系统约束：允许的 API、类型、参数范围、禁止事项、输出格式。
   - 现状：当前程序、当前分数、失败 case 摘要、约束 violation 统计。
   - 请求：生成 N 个候选变体，每个带修改理由与预期效果。
4. **静态检查**：AST/allowlist/类型/复杂度/禁止网络与文件访问。
5. **单测与属性测试**：边界输入、NaN/除零、空集合、极端值、资源上限。
6. **执行评估**：低保真先筛，高保真复验 top-k。
7. **更新种群/archive**：保留精英 + 多样性；失败案例回灌 prompt。
8. **每轮记录**：prompt、模型版本、原始输出、解析结果、编译结果、评估结果、失败原因。

### 5.4 最小伪代码（示意）

```python
population = [seed_program]
for round in range(R):
    parent = select(population)
    feedback = summarize(execute(parent, train_designs))
    candidates = llm_mutate(
        parent=parent,
        feedback=feedback,
        constraints=ALLOWED_API,
        n=4,
    )
    valid = [c for c in candidates if static_check(c) and unit_tests(c)]
    for c in valid:
        c.score = evaluate(c, train_designs)
    population = update(population + valid, keep_elites=5)
best = best_valid(population)
signoff_recheck(best)
```

### 5.5 关键设计点

- **表示**：优先受限 DSL 或参数化模板；L3 才允许生成代码。
- **反馈信号**：告诉 LLM 哪个设计/哪个阶段失败、失败类型（DRC、overflow、timeout），比只给一个总分更有效。
- **多样性**：多 prompt/多 island/不同温度；避免所有候选同质化。
- **预算**：限制 API calls、token、候选数、重试次数；与 GP/BO 基线做**同等评估预算**对照。
- **版本**：锁定模型名、日期、prompt hash；记录实际 API 返回。
- **安全**：沙箱、allowlist、超时、内存限制、输出 schema 校验、fallback。

### 5.6 在 EDA / skill 里怎么落地

- **L1 参数/触发条件进化**：最安全；例如进化“何时把 density 从 0.6 降到 0.5”。
- **L2 组合/停止条件进化**：例如选择 skill 序列和停止规则。
- **L3 结构/代码进化**：受限 DSL 中生成新代价函数/规则；必须通过 L0–L2 verifier。
- **Skill 契约**：每个生成候选都要带前置条件、后置不变式、fallback 和测试；否则不算 skill。
- 先让 LLM 做“选择和参数提议”，几何/数值仍交给 OpenROAD/DREAMPlace。

### 5.7 适用范围、前置条件与常见坑

**适用：** 有可靠 verifier、有文档/API 语义可以利用、评估成本可控；适合冷启动和结构创新。  
**前置条件：** 沙箱、allowlist、单测、统计评估协议、API 预算管理。  
**常见坑：**
- 没有 verifier 就执行 LLM 代码：不可控，禁止。
- 把测试设计写进 prompt：数据泄漏，泛化虚高。
- 只报告最好一次：LLM 方差大，必须多 seed/多次重复。
- API 模型漂移：用固定 eval 集监控 parse rate 和 selection quality。
- token 成本失控：缓存 prompt、批处理、早停、蒸馏小模型。

### 5.8 最小验收标准

在固定 API/tool 调用预算下，LLM 进化的最好程序/策略显著优于 BO/GP/人工基线；同时候选合格率（verifier pass rate）达到预设门槛，失败案例可复现。

---

## 路线 6：RL / 离线 RL 学习算子策略

### 6.1 一句话定位与类比

**定位：** 把“下一步选哪个算子/参数”建模成序贯决策问题（MDP），用奖励信号学习一个策略网络，而不是写显式规则。  
**类比：** 打游戏时根据当前局面选技能；离线 RL 相当于看历史录像学习，而不是亲自打很多局。

### 6.2 MDP 定义（EDA 版）

```
state  s：checkpoint + 状态卡 + 图特征（拥塞/时序/合法性/预算）
action a：skill_id + 参数 + horizon
transition T(s,a)：执行工具 + shield + checkpoint 回滚
reward r：gate 惩罚 + ΔPPA + 成本惩罚
policy π(a|s)：输入状态，输出动作分布
```

### 6.3 算法家族

| 家族 | 代表 | 数据要求 | 说明 |
|---|---|---|---|
| Online RL | PPO、DQN、A2C | 可反复交互 | 直接探索；EDA 上太贵 |
| Offline RL | CQL、IQL、TD3+BC | 一批历史轨迹 | 从日志学策略，避免在线试错 |
| Sequence modeling | Decision Transformer、Trajectory Transformer | 大量轨迹 | 把 RL 当条件序列生成 |
| Hierarchical RL | Options、HRL | 带宏动作的数据 | 与 skill/option 天然匹配 |
| Imitation | BC、DAgger | 专家/搜索轨迹 | 先学已有好策略，再 RL 微调 |

### 6.4 最小操作流程（离线优先）

1. **收集轨迹**：用 baseline/随机/已有 skill 在多个 checkpoint 上执行，存 `(s, a, r, s', done, propensity)`。
2. **奖励设计**：gate 违例给大负值；PPA 增量归一化；runtime/token 惩罚；避免只奖励 HPWL。
3. **训练离线 RL**：先用 IQL/CQL 等保守算法；用 validation 轨迹做 off-policy evaluation。
4. **策略校准**：检查 Q 值/回报预测是否校准；低置信区域不要部署。
5. **半离线 pilot**：在少量留出设计上跟 baseline 并行，shield 保护，仅当不劣化才采纳。
6. **迭代**：新轨迹回灌 replay buffer，逐步扩大自主比例。

### 6.5 最小伪代码（示意）

```python
replay = []                              # (s, a, r, s_next, done, propensity)
for episode in collect_with_mixture_policy(designs, n=1000):
    replay.append(episode)

agent = OfflineRLAlgorithm(state_dim, action_space)
for step in range(train_steps):
    batch = sample(replay)
    loss = agent.update_behaviour_regularized(batch)   # 保守正则
    log_q_calibration(agent, val_replay)

if passes_offline_ope(agent) and passes_shield_checks(agent):
    deploy_with_shield(agent, fallback=baseline_policy)
```

### 6.6 关键参数与设计点

- 折扣因子、回报归一化、保守正则系数、目标网络更新率、batch size。
- Action space：离散 skill 选择最稳；连续参数建议作为 skill 的内部参数，由更低层求解器处理。
- Reward shaping：potential-based shaping 可减少 reward hacking。
- Propensity logging：如果记录选择概率，可以做 IPS/DR 等离线评估；否则只能做受限的 replay。
- 保证安全：任何 RL 动作都先过 shield；失败回滚。

### 6.7 适用范围、前置条件与常见坑

**适用：** 已有大量 checkpoint 轨迹、问题确实是序贯的、action 空间可离散化、有稳定 reward。  
**前置条件：** replay 数据、reward 校准、OPE 方法、shield/fallback、评测方差控制。  
**常见坑：**
- 样本效率低：不要一上来就 online RL；先 ISAC/bandit。
- 分布漂移：旧轨迹上的策略在新工具版本/设计族上可能失效。
- Reward hacking：只优化代理指标会绕过 gate；必须用 signoff 做最终门禁。
- OPE 高估：离线评估与在线结果可能差很多；用独立小规模 pilot 验证。
- 长时程 credit assignment：EDA 决策影响可能延迟到 route/signoff，需要 n-step return 或 option 结构。

### 6.8 最小验收标准

离线策略在留出设计上不劣于 bandit/ISAC 基线；Q 值校准误差在门槛内；半离线 shield pilot 中 gate 通过率不下降；再考虑扩大部署。

---

## 路线 7：自动算法调度 / 参数调度（Algorithm Schedule）

### 7.1 一句话定位与类比

**定位：** 不选“某一个参数”，而是生成一条**按阶段展开的时序策略**：先跑什么、参数怎么变、达到什么条件就切换、什么时候停。  
**类比：** 做菜的火候曲线（先大火烧开、再小火慢炖、最后收汁），而不是只定一个“炉温=180℃”；或者健身的周期化训练计划。

### 7.2 它和单点参数搜索的区别

- 单点参数搜索：`config = (density=0.6, adjustment=0.2)`，整个 flow 固定。
- Schedule 搜索：`schedule = [(phase1, density=0.65, until=overflow<3), (phase2, timing_driven, steps=3), (phase3, routability_driven, until=done)]`。
- Schedule 更接近 EDA 工程师真实调 flow 的方式：**阶段之间可以切换策略**，而不是一个配置跑到底。

### 7.3 Schedule 的表示方式

| 表示 | 例子 | 适合 |
|---|---|---|
| 阶段列表 | `[阶段1: 参数A, 阶段2: 参数B]` | 阶段边界清晰 |
| 事件驱动 | “当 overflow 低于阈值就切到 timing 修复” | 需要运行时反馈 |
| 参数曲线 | density 从 0.65 线性降到 0.45 | 连续退火类策略 |
| 条件树 | 根据设计特征选不同 schedule | 设计异构 |
| 宏观 option 序列 | `skill1 → skill2 → skill3` | 与 04 的 option/horizon 结合 |

### 7.4 最小操作流程

1. **定义阶段集合与触发/停止条件**：每个阶段必须可被工具状态检测（例如 overflow、WNS、iteration count）。
2. **参数化 schedule**：阶段数、每阶段参数、步数/预算、切换阈值。
3. **可行性与预算过滤**：先剔除会超时、跑不完、违反硬约束的 schedule。
4. **在训练设计上搜索**：可用随机/BO/进化/PBT；评估 `schedule → gate + PPA + runtime`。
5. **跨设计验证**：schedule 必须在多个设计上稳定，而不是只对一个设计调得很好。
6. **选 Pareto 集**：不只选平均最好，也保留“最坏情况好”和“快速”的 schedule。

### 7.5 最小伪代码（示意）

```python
schedule = [
    {"phase": "global_place", "density": 0.65, "until": "iteration_done"},
    {"phase": "timing_refine", "weight_wns": 0.8, "steps": 3},
    {"phase": "routing_adjust", "gr_adjustment": 0.2, "until": "overflow_ok"},
]

def evaluate_schedule(schedule, design):
    state = open_checkpoint(design)
    for phase in schedule:
        state = apply_phase(state, phase)      # 写 config / 调 Tcl
        check_gate(state)                     # 失败则立即回滚
        if budget_exceeded(state):
            return penalty
    return parse_signoff_metrics(state)
```

### 7.6 关键参数

- 阶段数：建议 2–5，先少后多（阶段越多越容易过拟合）。
- 每段预算：步数、迭代次数、wall-clock、工具调用数。
- 触发阈值：overflow、WNS/TNS、iteration、utilization 等；必须有回退策略。
- 搜索预算：schedule 空间比参数空间大，评估次数要按维度增长。
- 跨设计稳健性：用最差设计表现和方差作为约束。

### 7.7 在 EDA 里怎么落地

- **placement schedule**：先低 density 做全局布局 → 提高 density 做合法化 → timing-driven refine → routability check。
- **routing schedule**：先全局布线粗调 → 根据 overflow 做 RRR → 调整 layer/via 代价 → 再详细布线。
- **CTS/repair schedule**：何时开 timing repair、跑几次、何时停止。
- **flow-level schedule**：阶段顺序和参数；这是最接近“工具使用策略”的路线，风险低、收益直接。
- **触发信号**：OpenROAD 的 overflow report、iteration log、STA 的 WNS/TNS；必须能从工具输出稳定解析。

### 7.8 适用范围、前置条件与常见坑

**适用：** 你已经有可靠的单阶段参数/代价函数，想在阶段之间做组合；EDA 流程天然适合 schedule。  
**前置条件：** 可中断/可恢复的 flow、稳定的触发信号解析、每阶段 checkpoint。  
**常见坑：**
- 阶段边界判断不可靠：事件触发条件要测试，必要时加超时兜底。
- schedule 过拟合：阶段数越多，越容易只对训练设计有效。
- 运行时爆炸：每增加一个阶段都可能显著增加 runtime，需要预算硬上限。
- 阶段间目标冲突：例如前面线长很好但后面 routability 崩溃；要用跨阶段 reward（如 GRT overflow）而不是只看当前阶段。
- 不可中断：没有 checkpoint 就不能做安全的 schedule 搜索。

### 7.9 最小验收标准

在相同总 tool-call 预算下，搜索出的 schedule 优于固定单点最优；在留出设计上的最差表现不显著变差；所有触发/回退逻辑可复现。

---

## 路线 8：形式化/约束合成 与 案例复用

这条路线包含两个差异较大的方向，分开讲。

### 8A. 形式化/约束合成（SAT / SMT / ILP / CP / CEGIS）

#### 8A.1 一句话定位与类比

**定位：** 用逻辑/约束求解器，在“满足规格”的前提下合成一个小程序、参数或方案；解的正确性可以由约束满足来保证。  
**类比：** 拼图时不是靠试错，而是把所有拼块关系和边界条件写成方程，交给求解器找出满足条件的摆法。

#### 8A.2 最小操作流程（以 CEGIS 为例）

1. **规格**：写明前置条件、后置条件、不变式。例如“合法化后任意两个单元不重叠”“所有 row 对齐”“固定单元位置不变”。
2. **编码**：把变量、约束、目标转成 SAT/SMT/ILP/CP 模型。
3. **求解**：求解器给出候选解（可能存在多个）。
4. **验证/反例**：用真实 checker 验证；若失败，把反例加入约束集。
5. **迭代**：CEGIS = CounterExample-Guided Inductive Synthesis，不断用反例修正。
6. **部署**：把合成的参数/规则作为 skill 的受限实现。

#### 8A.3 关键概念

- SAT：布尔可满足性问题；适合离散开关/二值决策。
- SMT：可满足性模理论；支持整数、实数、数组等；适合位置/约束混合问题。
- ILP/MIP：整数/混合整数线性规划；适合资源分配、层分配、小规模布局。
- CP：约束规划；适合排列、调度、传播式约束。
- CEGIS：反例引导的归纳合成；适合程序/规则合成。

#### 8A.4 在 EDA 里怎么落地

- 小规模 placement 合法化/行分配；小区域内的 swap 决策。
- global routing 的层分配、容量约束子问题。
- 修复规则：满足 DRC/间距/连通性约束的局部调整。
- 参数可行性：找出满足 timing guardband 和资源限制的参数区域。
- **不要**期待 ILP 直接解大规模 PPA；把它当“硬约束小算子的合成器”。

#### 8A.5 适用范围、前置条件与常见坑

**适用：** 问题有清晰数学结构、约束硬、规模小；正确性要求极高。  
**前置条件：** 会写约束模型；有求解器（Z3、Gurobi、CP-SAT 等）；有 checker 验证。  
**常见坑：** 规模爆炸、目标函数难线性化、编码错误导致“求解器说可行但实际非法”、超时无解。所有解都必须过真实 checker。

### 8B. 案例复用（CBR, Case-Based Reasoning）

#### 8B.1 一句话定位与类比

**定位：** 从历史成功案例中检索“和当前状态最像”的案例，把当时的策略适配过来；本质是带记忆的 kNN/检索。  
**类比：** 老工程师看到新设计说“这个和上次那个很像，上次用这套参数和修复顺序成了”，然后微调复用。

#### 8B.2 最小操作流程

1. **案例表示**：状态特征 + 当时动作/参数 + 结果 + 适用条件。
2. **相似度检索**：欧氏/余弦/树模型/GNN embedding；返回 top-k 案例。
3. **适配**：按规模/密度/工艺缩放参数，或删改动作序列。
4. **验证**：必须过 gate 和单次真实评估；不通过就回退。
5. **存储**：成功/失败案例都入库，失败案例用于避开坏动作。
6. **更新**：新结果回写案例库；过期案例降权。

#### 8B.3 在 EDA 里怎么落地

- 检索相似设计的全局布局参数；检索相似拥塞模式的 RRR 批次大小。
- 冷启动阶段没有数据时，可以先用规则/领域知识构造初始案例。
- 与 LLM selector 结合：LLM 读案例说明，但最终动作来自案例库的已验证条目。

#### 8B.4 适用范围、前置条件与常见坑

**适用：** 冷启动、设计族重复性高、可解释性要求较高。  
**前置条件：** 案例库、特征提取、相似度度量、验证器。  
**常见坑：** 相似不等于可迁移；案例过时（工具/PDK 变了）；只存成功案例导致选择偏差；检索特征和动作有效性弱相关。

### 8C. 最小验收标准

形式化合成：约束满足率、修复成功率、真实 checker 通过率；案例复用：检索命中率、与全局最优/默认策略相比的 regret、跨设计族泛化。

---

## 9. 八条路线横向对比

| 路线 | 搜索空间 | 样本效率 | 正确性风险 | 可解释性 | 实现难度 | EDA 适配 | 首选场景 |
|---|---|---|---|---|---|---|---|
| 1 参数搜索 | 连续/离散参数 | 中 | 低 | 中 | 低 | 高 | 快速工程收益、强基线 |
| 2 ISAC | 配置选择器 | 高（离线） | 低 | 中 | 中 | 高 | 设计异构、推理要便宜 |
| 3 GP/DSL | 表达式/规则 | 低-中 | 中 | 高 | 中 | 中 | 需要可读的新评分函数 |
| 4 QD/MAP-Elites | 策略库 | 低 | 中 | 中 | 中 | 中-高 | 设计族多样、要策略库 |
| 5 LLM 进化 | 代码/DSL | 低 | 高（需 verifier） | 中 | 中-高 | 中-高 | 冷启动、结构创新 |
| 6 RL/离线 RL | 序贯策略 | 低-中 | 中-高 | 低 | 高 | 中 | 数据充足后的长期策略 |
| 7 Schedule | 阶段序列 | 中 | 中 | 高 | 中 | 很高 | EDA flow 调优、跨阶段 |
| 8 形式化+案例 | 约束解/历史案例 | 高/中 | 低（形式化）/中（案例） | 高 | 中 | 中-高 | 正确性优先、冷启动 |

**组合建议：**
- 工程强基线 = 路线 1 + 路线 2；
- 多样性库 = 路线 4 + 路线 7；
- 结构创新 = 路线 3 + 路线 5；
- 数据足够后 = 路线 6；
- 正确性/冷启动 = 路线 8。

---

## 10. 建议的学习与实施顺序（EDA Phase 0.5）

1. **第 0 步：搭评估 harness**  
   固定设计、固定 checkpoint、固定 seed、固定预算；把每次 trial 的 config/动作/指标/日志落盘。没有这个，后面全是空谈。
2. **第 1 步：Random Search + OpenLane 参数**  
   先做 10–20 trials 的小实验，验证评估函数、并行、日志和 gate 解析。
3. **第 2 步：TPE/SMAC + 小设计**  
   用 BO 建立第一组强基线；学习 acquisition、预算、条件空间。
4. **第 3 步：ISAC/排序选择器**  
   在已有参数数据上训练跨设计选择器；对比全局最优。
5. **第 4 步：案例复用 + Schedule**  
   案例库低风险；schedule 最贴 EDA flow，收益通常明显。
6. **第 5 步：MAP-Elites**  
   建按设计族分桶的策略档案，和 ISAC/bandit 选择器对接。
7. **第 6 步：GP/DSL 规则搜索**  
   在有了快速评估器之后再做，先限制 DSL 和深度。
8. **第 7 步：LLM 程序进化**  
   放在最后，但一旦 verifier 成熟可以快速产生结构候选；与 GP/BO 做预算对齐。
9. **第 8 步：离线 RL**  
   等 replay 数据、reward 校准、OPE 和 shield 都成熟后再考虑。

> 顺序原则：**先建立强基线，再上 LLM/RL**。否则你无法判断提升来自方法本身，还是来自更多评估预算。

---

## 11. 术语速查

| 术语 | 含义 |
|---|---|
| Black-box optimization | 只知道输入输出、不知道梯度的优化 |
| Surrogate model | 代理模型，用便宜近似替代昂贵真实评估 |
| Acquisition function | 贝叶斯优化中决定下一个采样点的函数（EI/UCB 等） |
| Fidelity | 评估保真度；低保真便宜，高保真贵 |
| Budget | 预算：评估次数、wall-clock、tool calls、tokens |
| Trial | 一次完整的候选配置评估 |
| Regret | 和 oracle 最好选择的差距，越小越好 |
| Algorithm configuration | 算法参数配置问题 |
| ISAC | 逐实例算法配置：按实例特征选配置 |
| Genome / phenotype | 进化算法中的编码 / 解码后的规则 |
| Fitness | 适应度，候选解的质量分数 |
| Behavior descriptor | 行为描述子，QD 中用于分桶的坐标 |
| Elite | 每个行为桶里当前最好的解 |
| Archive | QD 保存所有 elite 的容器 |
| CEGIS | 反例引导的归纳合成 |
| CBR | 基于案例的推理 |
| Offline RL | 从固定历史数据学习策略 |
| Replay buffer | 存放历史轨迹的数据集 |
| OPE | Off-policy evaluation，离线策略评估 |
| Shield | 安全守卫，执行前检查/失败回滚 |
| Gate | 硬约束门禁（DRC/LVS/连通性/时序） |
| HVI | Hypervolume Improvement，多目标帕累托体积改进 |
| CVaR | 条件风险价值，衡量尾部最差情况 |

---

## 12. 统一最小实验模板

无论用哪条路线，建议都用同一套外层循环，这样不同方法才可以公平比较：

```python
def run_method(method, designs, budget):
    records = []
    while budget_remaining(budget):
        proposal = method.propose(records)          # 可能是参数/规则/schedule/策略
        if not method.validate(proposal):
            records.append(failed_record(proposal))
            continue
        result = evaluate_on_checkpoints(proposal, designs)
        records.append(result)
        method.update(records)
    return summarize(records, with_cis=True)
```

每次评估至少记录：

```text
trial_id, method, version, seed, design, checkpoint,
config/action hash, budget_used, fidelity,
gate: {drc, lvs, connectivity, overlap, timing},
metrics: {wns, tns, power, area, wirelength, overflow, runtime},
status, log_path
```

**统计要求：**
- 同方法多 seed；
- 跨设计留出；
- 报告均值、中位数、最差、95% 置信区间；
- 所有方法用相同总预算；
- 结论必须包含“失败/无效案例”，不能只报成功。

**适用范围提醒：**
- 本文所有方法都只负责“生成候选”或“选择候选”，**最终正确性仍由 04 的 verifier 栈、guard 和 signoff 门禁保证**。
- 不要因为用了形式化/BO/LLM 就跳过 DRC/LVS/STA。
- 参数名、默认值和运行命令随 OpenLane/OpenROAD 版本变化，执行前以当前环境文档和 `help` 输出为准。

---

## 附录 A：在线筛选 10 条路线一句话（详细版可另开文件）

| 选择器 | 一句话 | 适合 |
|---|---|---|
| 静态最优 | 总是选平均最好的 | baseline |
| 逐实例选择/排序 | 按设计特征预测最佳 | 离线数据充足 |
| 上下文 bandit | 在线探索+利用，带置信区间 | 在线决策多 |
| 贝叶斯优化 | 代理模型+acquisition 选候选 | 评估昂贵 |
| 经典 hyper-heuristic | 轻量概率/计分选择 | 低开销基线 |
| RL/离线 RL | 学序贯策略 | 数据足够 |
| Racing/Successive Halving | 快筛差候选，省预算 | 候选多、可早停 |
| Algorithm schedule | 选一段策略模板 | 跨阶段 |
| LLM/RAG | 读状态卡语义选择 | 冷启动、语义状态 |
| Meta-learning | 少样本适应新设计族 | 新工艺/新设计 |

建议的第一组最小对照：**生成器 G1 Random + G2 BO/SMAC；选择器 S0 Static + S1 ISAC/ranking + S2 LinUCB/TS + S4 dsv4-flash**。先把这个矩阵跑通，再决定是否把 LLM 自进化作为主路线。
