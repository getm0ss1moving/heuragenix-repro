# 除了 Skill 自进化：算法生成与在线筛选的备选路线（建议稿 v0.1）

**日期：** 2026-09-20  
**配套：** `04_EDA迁移与Skill自进化设计建议.md`  
**问题：** 除了“创建 skill 让 LLM 自进化 + LLM 在线选择”，还有哪些生成优化算法或在线筛选算法的办法？

---

## 0. 先厘清：Skill 在哪一层？

“Skill”不是生成方法，也不是选择方法，它本质是**动作接口/可执行 option 的抽象**。  
真正的方法空间有三个正交维度：

| 维度 | 可选方案 | 你当前方案 |
|---|---|---|
| **算法表示** | 参数向量 / 规则 DSL / AST / 完整代码 / 策略网络 / 调度序列 | Skill（参数 + 受限可执行体） |
| **生成机制** | 随机搜索 / 贝叶斯优化 / 进化 / 程序合成 / LLM 变异 / RL / QD | LLM mutation + verifier |
| **在线选择** | 静态最优 / 回归排序 / 上下文 bandit / RL / BO / racing / LLM | LLM + TTS |

所以“除了 skill”的正确问法是：  
**如果不把动作封装成 skill，还有哪些表示、生成、选择机制？它们与 skill 方案各自适合什么场景？**

建议把三者做成可插拔模块：

```
Representation  ×  Generator  ×  Selector  ──> 受 verifier/shield 保护地执行
   ├ 参数向量        ├ 随机/网格/BO      ├ 静态最优
   ├ DSL/规则        ├ GP/语法进化       ├ 代价敏感排序
   ├ 代码/AST        ├ LLM 程序进化      ├ 上下文 bandit
   └ 策略网络        └ RL/离线RL/QD      └ RL/BO/racing/LLM
```

---

## 1. 算法/启发式生成：8 条备选路线

### 1.1 黑盒参数/配置搜索（风险最低，优先做）

- **方法**：Random Search、Grid、TPE（Optuna）、SMAC3、BOHB、DEHB、CMA-ES、PBT（Population-Based Training）。
- **对象**：OpenROAD/OpenLane 的参数：`PL_TARGET_DENSITY`、`PL_WIRELENGTH_COEF`、`GRT_ADJUSTMENT`、`GRT_OVERFLOW_ITERS`、layer adjustment、detailed route effort、CTS 参数等。
- **优点**：不生成代码，正确性风险几乎为零；对 EDA 这类“参数敏感、评估昂贵”的问题很合适；有成熟实现。
- **缺点**：不会发明新算子，只能在已有参数空间内优化。
- **EDA 适配**：★★★★★，应作为所有“LLM 自进化”的强基线。

### 1.2 逐实例算法配置 / 参数选择（ISAC）

- **方法**：Per-Instance Algorithm Configuration。先用设计特征（面积、利用率、宏数量、congestion 分布、时序紧张度）训练一个 cost-sensitive 模型，预测“某参数组合在该设计上的表现”，再按实例选择参数。
- **对象**：global config vs per-design config；也可以是“给 placement 选 density、给 routing 选 adjustment”。
- **优点**：推理便宜、可解释、样本效率高；比“一个全局参数打天下”更贴近 EDA 异构设计。
- **缺点**：需要先有跨设计的历史数据。
- **EDA 适配**：★★★★★，建议作为在线选择的第一条基线。

### 1.3 语法/程序搜索（GP / 语法进化）

- **方法**：Genetic Programming、Grammatical Evolution、Cartesian GP、表达式树搜索。
- **对象**：代价函数、net ordering 规则、move acceptance 规则、参数调度函数、局部搜索评分。
- **优点**：可以发现新规则；如果 DSL 小，结果可读；与 EDA 的“策略/评分函数”天然契合。
- **缺点**：稀疏奖励、评估贵、容易过拟合；需要 verifier 和强约束。
- **EDA 适配**：★★★☆☆，适合作为“结构生成”的中间层。

### 1.4 Quality-Diversity / MAP-Elites

- **方法**：MAP-Elites、CVT-MAP-Elites、Novelty Search、QD-RL。
- **对象**：生成一整个**多样化 skill/策略档案**，按设计特征和行为的 descriptor 分桶存档，而不是只保留一个最优策略。
- **优点**：EDA 设计异质性极强，单一最优策略往往不鲁棒；QD 天然产出“按设计族特化”的 portfolio。
- **缺点**：需要大量评估；archive 管理和 descriptor 设计是关键。
- **EDA 适配**：★★★★☆，建议与 selector 配套使用。

### 1.5 LLM 程序进化（你已有路线的泛化）

- **方法**：FunSearch、EoH、ReEvo、AlphaEvolve、HeurAgenix；你的“skill 自进化”是其类型化/契约化版本。
- **对象**：新算子、Tcl/Python 工具调用模板、状态特征提取、策略组合。
- **关键差异**：必须把 verifier 放在回路里；代码必须在 sandbox/allowlist 中执行。
- **优点**：能读文档、复用语义先验、冷启动好；可以生成“工具使用策略”而不只是数值参数。
- **缺点**：易幻觉、版本漂移、成本高；没有 verifier 时不可信。
- **EDA 适配**：★★★★☆（加约束后），建议和 BO/GP 做预算对齐对照。

### 1.6 RL / 离线 RL 学习算子策略

- **方法**：RL over primitives（PPO/DQN）、Hierarchical RL/Options、离线 RL（CQL/IQL）、Decision Transformer、Trajectory Transformer。
- **对象**：序贯决策的策略本身，而不是显式规则；可以学“何时选哪个算子”。
- **优点**：能捕捉长时程效应；离线 RL 可以直接用 checkpoint replay 数据，避免在线试错。
- **缺点**：样本效率、分布漂移、reward hacking；EDA 一次 rollout 太贵。
- **EDA 适配**：★★★☆☆，适合在已有大量 replay 数据后做。

### 1.7 自动算法调度 / 参数调度（Algorithm Schedule）

- **方法**：AutoML 调度搜索、PBT、进化算法搜索“阶段 × 参数 × 持续步数”的 schedule。
- **对象**：不是选一个算法，而是生成一条“先跑什么参数、再切什么参数”的序列；例如先低 density 全局布局，再 timing-driven 修复，再 routability-driven。
- **优点**：比单点参数更接近 EDA 实际 flow 调优；与你的 option/horizon 设计天然衔接。
- **缺点**：搜索空间更大，需要多保真评估。
- **EDA 适配**：★★★★★，是很值得优先探索的“中间粒度”。

### 1.8 形式化/约束合成与案例复用

- **形式化合成**：用 SAT/ILP/CEGIS/sketch synthesis 在小范围内合成满足前置/后置条件的算子；优势是可给正确性证书，但扩展性有限。
- **案例复用（CBR）**：从相似设计/状态检索成功参数或算子序列，做 compose/adapt；低风险、可解释，适合冷启动。
- **EDA 适配**：★★★☆☆，适合作为 repair/合法性等强约束小算子的合成。

---

## 2. 在线算法筛选：10 条备选路线

### 2.1 静态最优 / 全局默认

- 选历史平均最好的一个算法/参数。  
- 作用：必备 baseline；如果没有显著超过它，说明 selector 或 skill 库价值有限。

### 2.2 逐实例算法选择（cost-sensitive ranking / regression）

- **SATzilla 式路线**：提取实例特征 → 用随机森林/XGBoost/排序模型预测每个候选算法的性能 → 选预测最优。
- **变体**：pairwise ranking、cost-sensitive classification、meta-learning。
- **优点**：训练好后推理极便宜；样本效率高；可解释；适合你这种“决策点多、tool call 贵”的场景。
- **缺点**：对分布漂移敏感，需要定期重训。
- **EDA 适配**：★★★★★，建议做第一个非 LLM selector 基线。

### 2.3 上下文 bandit（online selection 的主力候选）

- **方法**：LinUCB、Thompson Sampling、NeuralTS、EXP3/EXP4、constrained bandit、combinatorial semi-bandit。
- **特点**：在“利用已知好 skill + 探索潜在更好 skill”之间平衡；支持成本感知和约束（安全 bandit）。
- **优点**：在线自适应、可给 regret 与置信区间；比 RL 轻得多。
- **缺点**：长时程 credit assignment 弱；冷启动需要先验或离线数据。
- **EDA 适配**：★★★★★，尤其适合“参数级 skill + 多设计 checkpoint”场景。

### 2.4 贝叶斯优化 / 多保真 BO

- **方法**：GP-EI/UCB、SMAC、BOHB、multi-fidelity BO、trust-region BO。
- **特点**：把“选哪个算法/参数”当成黑盒优化问题，用 surrogate + acquisition 决策；多保真版本可先跑便宜阶段再决定是否 signoff。
- **优点**：样本效率高，特别适合单次评估昂贵的 EDA；天然支持不确定性和探索。
- **缺点**：高维/离散/组合空间需要结构化核或分解。
- **EDA 适配**：★★★★★。

### 2.5 经典 selection hyper-heuristic 在线学习

- **方法**：Choice Function、Adaptive Pursuit、Probability Matching、Tabu Search、Reinforcement Learning 版 hyper-heuristic。
- **特点**：轻量、代码简单、对小规模反馈友好；是 HeurAgenix 的“经典前身”路线。
- **优点**：实现快、可解释、作为基线便宜。
- **缺点**：状态表示弱、难以利用复杂状态卡。
- **EDA 适配**：★★★☆☆，适合做低开销 baseline。

### 2.6 RL / 离线 RL selector

- **方法**：PPO/DQN 做序贯选择；离线 RL（CQL/IQL）从日志学习；Decision Transformer 把 selection 当序列建模。
- **优点**：考虑跨步长时程影响；离线版本不需要在线试错。
- **缺点**：EDA 数据昂贵、非平稳、reward 设计难；容易高估策略。
- **EDA 适配**：★★★☆☆，在有 replay 数据后可尝试。

### 2.7 Racing / 逐步减半 / best-arm identification

- **方法**：F-Race、Successive Halving、Hyperband、best-arm identification、MCTS lookahead。
- **对象**：候选 skill 很多时，如何分配评估预算，尽快淘汰差的、保留好的。
- **优点**：直接降低 EDA 评估成本；与多保真天然结合。
- **缺点**：需要早停指标与可信的部分评估。
- **EDA 适配**：★★★★☆。

### 2.8 Algorithm Schedule / 组合器

- 在线选择不一定是单步；可以选“当前段采用哪个 schedule 模板”，把 option 序列作为宏观动作。  
- 对应 HeurAgenix 的 M 步连续执行，但 horizon 由事件/预算动态决定。

### 2.9 LLM/RAG/案例式 selector（你当前方案）

- 状态卡 + skill cards，dsv4-flash 输出选择；可用 experience bank 做 RAG。
- **优点**：冷启动好、能理解语义状态、能处理非结构化 EDA 信息、和人类可解释的 rationale。
- **缺点**：不可靠、成本高、版本漂移；必须用 JSON schema、缓存、校准、fallback。
- **建议定位**：不是唯一 selector，而是 selector 集成中的一个“专家”，与 bandit/ISAC 做 gating/ensemble。

### 2.10 Meta-learning / 少样本自适应

- **方法**：MAML、Reptile、proto-net、in-context learning（LLM 天然属于这一类）。
- **场景**：新工艺/新设计族只有少量评估时，快速适应。
- **EDA 适配**：★★★☆☆，研究性强。

---

## 3. EDA 约束下的推荐组合（不要二选一）

| 层次 | 推荐做法 | 理由 |
|---|---|---|
| 表示层 | Skill/option + 参数/DSL | 提供动作抽象和 verifier 接口 |
| 生成层-首版 | 参数搜索（SMAC/BO/CMA-ES）+ 少量人工 skill | 低风险、快速验证度量闭环 |
| 生成层-进阶 | LLM/DSL 演化 + GP + QD archive | 探索结构创新，和参数线预算对齐对比 |
| 选择层-首版 | 静态最优 / cost-sensitive ranking / LinUCB | 便宜、可统计、可做基线 |
| 选择层-进阶 | 离线 RL + MCTS/TTS + LLM gating | 处理长时程、冷启动与语义状态 |
| 验证层 | L0-L3 多保真 + shield/rollback | 保证正确性，控制评估成本 |
| 目标层 | 约束多目标 + HVI/CVaR | 对齐 timing/power/area 联合优化 |

**推荐的最小可行对照实验（EDA Phase 0.5）：**

同一批 EDA checkpoint 上对比，统一 tool-call/runtime 预算：

```
生成器：
  G0 人工默认参数
  G1 Random Search
  G2 BO/SMAC（参数级）
  G3 CMA-ES / PBT（参数调度）
  G4 GP/DSL 规则搜索
  G5 LLM skill 演化（你的方案）
选择器：
  S0 全局最优参数
  S1 cost-sensitive ranking / ISAC
  S2 LinUCB / Thompson Sampling
  S3 经典 hyper-heuristic（adaptive pursuit）
  S4 dsv4-flash LLM selector
  S5 离线 RL / Decision Transformer

评测：固定预算下 PPA/gate、time-to-target、sample efficiency、跨设计族泛化、最差 case。
```

**核心问题变成：**  
在相同的 EDA 评估预算（例如 50/100/200 次 flow 调用）下，哪种“生成器 × 选择器”组合能最快达到最优 PPA 且不破 gate？  
如果 LLM 自进化打不过 BO+bandit，就应该把 LLM 降级为“结构变异提议器 + 冷启动专家”，而不是唯一引擎。

---

## 4. 与你现有 Skill 方案的关系

- Skill 依然有价值：它是**统一动作接口**，让 BO、bandit、RL、LLM 都能在同一套动作上比较。
- 建议把 `04` 的架构拆成三个可替换模块：
  1. **Skill Library / Representation**：参数模板、DSL、代码；
  2. **Generator**：随机/BO/GP/LLM/RL/QD；
  3. **Selector**：静态/ISAC/bandit/RL/BO/racing/LLM。
- 最自然的演进路线：
  1. **用 skill 接口收集 replay 数据**；
  2. 用 **cost-sensitive ranking / bandit** 做选择器基线；
  3. 用 **BO/CMA-ES** 做参数生成基线；
  4. 再把 LLM 自进化加上去，和基线做**预算对齐**对照；
  5. 最后做生成-选择联合演化（co-evolution），并处理非平稳性。
- 论文贡献点也会更清楚：不是“用了 LLM”，而是证明了**在 EDA 昂贵、多目标、强约束环境下，验证引导的混合生成/选择架构**优于单一方法。

---

## 5. 风险提示

- 不要在没有强基线（BO、bandit、ISAC）的情况下声称 LLM 自进化有效。
- 不要把有限 signoff 样本浪费在明显的差候选上：先用 racing/多保真淘汰。
- 不要用单一设计/随机实例切分证明泛化；用 design family 留出。
- bandit/RL 必须记录 propensities，否则无法做可信的离线评估。
- LLM selector 必须校准与可弃权（abstain），低置信走 bandit/基线。
- 多目标优化不要只报平均值；最差 case 和 gate 通过率同等重要。
